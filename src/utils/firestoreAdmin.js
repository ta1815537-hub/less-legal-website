// Utility for Cloudflare Worker to call Firestore REST API as Admin
export async function getFirestoreAdminToken(env) {
  // requires env.FIREBASE_CLIENT_EMAIL, env.FIREBASE_PRIVATE_KEY
  return "placeholder";
}
